# leonslab

## Workflow

- **Default branch:** `claude/create-portfolio-site-1nPvv` (es gibt kein `main`).
- **Bei kosmetischen Änderungen** (Texte, Wording, kleine Style-Tweaks): direkt in den Default-Branch mergen und pushen — kein PR-Review nötig.
- Für funktionale/strukturelle Änderungen weiterhin Feature-Branch + Review.
